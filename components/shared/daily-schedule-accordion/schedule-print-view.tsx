import { Schedule } from '@/types/day'
import React from 'react'

export default function SchedulePrintView({
    scheduleData
}: {
    scheduleData: Schedule
}) {
    return (
        <div>SchedulePrintView</div>
    )
}
